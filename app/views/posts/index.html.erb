<h1>Whisper一覧</h1>
<p><%= notice %></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= link_to "詳細を確認する", post_path(post.id) %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to '新しくWhisperする', new_post_path %>